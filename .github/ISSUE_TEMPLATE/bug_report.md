---
name: Bug report
about: A template renders invalid JSON-LD, fails Google Rich Results, or doesn't match the schema.org spec
title: '[bug] '
labels: bug
assignees: ''
---

### What happened?

<!-- Which template, what you expected, what you got. -->

- Template @type involved (e.g. `Product`, `FAQPage`, `Organization`):
- File path:
- Validator output (`npm run validate`) or external validator URL:

### How to reproduce

1. Steps you took (copied which template, replaced which placeholders):
2. Example JSON-LD that exhibits the problem:

```json
<!-- paste here -->
```

3. Validator complaint or AI parser behaviour observed:

### Environment

- `@geosuite/schema-templates` version (`npm list @geosuite/schema-templates` or git SHA):
- Node version (`node --version`):
- Validator used (Google Rich Results, schema.org validator, our local `validate.js`):

### Anything else?

<!-- Link to schema.org spec, Google docs, AI assistant output that shows
the problem in the wild. -->
